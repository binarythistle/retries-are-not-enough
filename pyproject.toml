[project]
name = "retries-are-not-enough"
version = "0.1.0"
description = "Phase 1: a naive two-call LLM support-ticket pipeline"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0",
    "python-dotenv>=1.0",
    "temporalio>=1.31",
]
